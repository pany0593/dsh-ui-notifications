# dsh-system-notifications bundle patch: inserts the plugin row over the
# profile root, making the package installable via `dsh plugin add`.

- insert:
    - id: ui-notifications
      name: '@deepseek-ai/dsh-client-ui-notifications'
